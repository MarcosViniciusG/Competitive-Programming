#include<bits/stdc++.h>

using namespace std;
using ll = long long;
using vll = vector<ll>;

int main() {
    ll t, n, m, k, w, ai;
    cin >> t;

    while(t--) {
        cin >> n >> m >> k;
        cin >> w;
        
        for(ll i=0; i<w; i++)
            cin >> ai;
    }

    return 0;
}