#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using pll = pair<ll, ll>;
using vll = vector<ll>;
using vpll = vector<pll>;
#define endl '\n'

signed main() {
    string N;
    getline(cin, N);

    cout << N[1] << N[2] << N[0] << " " << N[2] << N[0] << N[1] << '\n';

    return 0;
}