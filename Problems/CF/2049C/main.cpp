#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using vll = vector<ll>;
using pll = pair<ll, ll>;
using vpll = vector<pll>;
using vi = vector<int>;

signed main()
{
    cin.tie(nullptr)->sync_with_stdio(false);
    ll t, n, x, y;
    cin >> t;
    while (t--)
    {
        cin >> n >> x >> y;
       
    }

    return 0;
}