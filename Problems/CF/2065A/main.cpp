#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using vll = vector<ll>;
using pll = pair<ll, ll>;
using vpll = vector<pll>;

signed main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    string w;
    
    ll t;
    cin >> t;

    while(t--){
    cin >> w;
    


    string ans {w.begin(), w.end()-2};
    cout << ans << 'i' << '\n';
    }

    return 0;
}