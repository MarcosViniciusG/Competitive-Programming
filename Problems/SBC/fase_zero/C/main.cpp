#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
    ll N, M, Li, Ri, i, j, P, maxP=1;
    vector<ll
    vector<pair<ll>> Ls;
    vector<ll> Rs;
    cin >> N >> M;
    for(i=0; i<M; i++)
    {
        
        cin >> Li >> Ri;
        tipos.push_back()
    }

    for(i=1; i<=N+1; i++)

    return 0;
}